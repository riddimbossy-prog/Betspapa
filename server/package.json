{
  "name": "betspapa-api",
  "version": "1.25.3",
  "private": true,
  "description": "BetsPapa v1.25.3 serves the next qualified SportyBet Flash slate when today's strict audit is a full skip.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
