{
  "name": "betspapa-api",
  "version": "1.13.0",
  "private": true,
  "description": "BetsPapa Render API with prediction engines, Supabase accounts, watchlists, push alerts, pipeline recovery and grading.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "vapid": "node ../scripts/generate-vapid-keys.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "web-push": "^3.6.7"
  }
}
