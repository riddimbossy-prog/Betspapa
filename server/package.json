{
  "name": "betspapa-api",
  "version": "1.1.0",
  "private": true,
  "description": "BetsPapa Render API with Supabase health checks and HT/FT prediction engine.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
