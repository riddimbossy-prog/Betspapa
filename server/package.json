{
  "name": "betspapa-api",
  "version": "1.24.0",
  "private": true,
  "description": "BetsPapa v1.24.0 keeps public boards picks-only and adds direct Papa, Safer, Aggressive and Athena navigation for phones, tablets and Z Fold.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
