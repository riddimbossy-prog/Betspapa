# What to Expect After v1.18.3

1. Opening betspapa.com goes straight to Papa's Pick.
2. Installed PWA launches straight to Papa's Pick.
3. The old Papa's Pick URL redirects to the root without losing query parameters.
4. Z Fold cover screen uses one readable card column.
5. Z Fold inner screen and compact tablets use two columns when the viewport permits.
6. Tablet landscape can use three card columns.
7. Bottom navigation remains visible through tablet and Fold widths.
8. No prediction rows need regeneration because the engine version is unchanged.
9. A one-time reset is required to replace the former cached home page.
